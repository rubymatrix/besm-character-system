<%# app/views/character_sheets/edit.html.erb %>

<div class="flex h-full overflow-hidden" data-controller="sidebar">
  <%= render "shared/sidebar" %>
  <div class="flex flex-col flex-1 min-w-0 overflow-auto">
    <% content_for :title, "Edit Character" %>

    <div class="max-w-7xl mx-auto px-4 md:py-12 py-6 w-full">
      <h1 class="text-3xl font-semibold font-heading tracking-tight mb-8 text-zinc-900 dark:text-zinc-100">
        Edit Character
      </h1>
      
      <%= render "form", character_sheet: @character_sheet %>
    </div>
  </div>
</div>